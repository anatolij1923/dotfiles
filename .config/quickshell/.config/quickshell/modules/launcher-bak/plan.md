Этап 1: Фундамент (Utils & Base)

Прежде чем делать логику, нужно подготовить инструменты поиска.
1.1. Зависимости JS

Нам нужны скрипты для нечеткого поиска (Fuzzy Search), так как нативный String.includes — это скучно.

    Задача: Найти/скачать файлы fuzzysort.js и fzf.js (из репо Caelestia или npm).

    Куда: qs/utils/scripts/

1.2. Базовый компонент Searcher.qml

Это будет "движок", который мы будем переиспользовать для всего (аппы, обои, команды).

    Куда: qs/utils/Searcher.qml

    Суть: Это не Singleton. Это компонент (QtObject или Item), который принимает список данных и строку поиска, а возвращает отфильтрованный список.

    API:

        property var list: Входной массив данных.

        function query(text): Возвращает отфильтрованный массив.

⚙️ Этап 2: Провайдеры (Services)

Создаем "Поставщиков данных". Каждый отвечает за свою область.
2.1. LauncherCore.qml (Brain / Router)

Главный мозг. Лаунчер будет общаться только с ним.

    Куда: qs/modules/launcher/services/LauncherCore.qml (Singleton)

    Логика:

        Хранит currentMode ("apps", "walls", "actions").

        В функции input(text) проверяет префиксы (:, w , =).

        Перенаправляет запрос в нужный Провайдер.

        Наполняет единую ListModel результатами.

2.2. Apps.qml (Provider)

    Куда: qs/modules/launcher/services/Apps.qml (Singleton)

    Логика: Использует твой старый добрый AppSearch или наследует Searcher. Возвращает список приложений.

2.3. Actions.qml (Provider)

    Куда: qs/modules/launcher/services/Actions.qml (Singleton)

    Логика: Берет команды из конфига (Reboot, Shutdown, Reload Shell).

    Фича: Срабатывает на префикс : (или просто в общем поиске, если хочешь).

2.4. Wallpapers.qml (Provider)

    Куда: qs/modules/launcher/services/Wallpapers.qml (Singleton)

    Логика:

        Сканирует папку с обоями (Quickshell.exec("ls", ...)) при инициализации.

        Фильтрует список по названию файла.

        Возвращает объекты с путями к картинкам.

🖼️ Этап 3: Визуал (Items / Delegates)

Создаем внешний вид для разных типов результатов.
3.1. LauncherItem.qml (Base UI)

Базовый контейнер (фон, ховер, клик).

    Куда: qs/modules/launcher/items/LauncherItem.qml

    Свойства: radius, color, onClicked.

3.2. AppItem.qml

Для приложений и действий.

    Куда: qs/modules/launcher/items/AppItem.qml

    Контент: Иконка (слева) + Название (жирно) + Описание (мелко).

3.3. WallpaperItem.qml

Для обоев.

    Куда: qs/modules/launcher/items/WallpaperItem.qml

    Контент:

        Превью картинки (Image с PreserveAspectCrop).

        Название файла поверх картинки или сбоку.

🚀 Этап 4: Сборка (Main UI)

Собираем всё вместе в главном окне.
4.1. Launcher.qml (Rewrite)

    Куда: qs/modules/launcher/Launcher.qml

    Структура:

        StyledTextField:

            onTextChanged: вызывает LauncherCore.input(text).

            onAccepted: вызывает LauncherCore.activate(currentIndex).

        ListView:

            model: LauncherCore.results.

            delegate: Loader.

    Магия Loader'а:
    code Qml


    Loader {
        sourceComponent: {
            if (model.type === "wall") return wallpaperComp
            return appComp
        }
    }



✅ Definition of Done (Чек-лист)

Зависимости (fzf, fuzzy) лежат в utils.

Searcher работает и фильтрует массивы.

LauncherCore умеет переключать режимы по префиксу w .

Wallpapers сканирует папку и отдает список файлов.

Launcher показывает список.

    Если пишу firefox -> Вижу AppItem.

    Если пишу w forest -> Вижу WallpaperItem с картинками.

Клик по обоине меняет обои (через swww или твой конфиг).
